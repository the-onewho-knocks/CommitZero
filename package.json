{
  "name": "commitzero",
  "displayName": "commitzero",
  "description": "AI-powered VS Code extension that generates intelligent commit messages using Gemini API.",
  "version": "1.0.2",
  "publisher": "HardikBorse",

  "icon": "assests/ok.png",

  "engines": {
    "vscode": "^1.109.0"
  },

  "categories": [
    "Other"
  ],

  "main": "./src/extension.js",

  "activationEvents": [
    "onStartupFinished",
    "onCommand:commitzero.generateCommit"
  ],

  "contributes": {

    "commands": [
      {
        "command": "commitzero.generateCommit",
        "title": "Generate Commit Message",
        "icon": {
          "light": "assests/pngegg.svg",
          "dark": "assests/pngegg.svg"
        }
      }
    ],

    "menus": {
      "scm/title": [
        {
          "command": "commitzero.generateCommit",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ]
    },

    "configuration": {
      "title": "commitzero",
      "properties": {

        "commitzero.geminiApiKey": {
          "type": "string",
          "default": "",
          "description": "Gemini API key used by commitzero to generate commit messages.",
          "scope": "application"
        },

        "commitzero.geminiModel": {
          "type": "string",
          "default": "models/gemini-2.5-flash",
          "description": "Gemini model used for commit message generation.",
          "scope": "application"
        }

      }
    }

  },

  "repository": {
    "type": "git",
    "url": "https://github.com/the-onewho-knocks/commitgenie"
  },

  "dependencies": {
    "axios": "^1.7.0"
  },

  "devDependencies": {
    "@types/vscode": "^1.109.0"
  }
}